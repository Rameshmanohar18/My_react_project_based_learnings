// const server = require("./server");
// const socket = require("./socket");

const server = require("./server");
const socket = require("./socket"); // socket will attach to server

const PORT = process.env.PORT || 4040;

// server.listen(PORT, () => {
//   console.log(`🚀 Server running on port ${PORT}`);
// });
